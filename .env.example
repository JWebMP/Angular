# Toggle Angular TypeScript processing (default: disabled)
JWEBMP_PROCESS_ANGULAR_TS=true
# System property equivalent: jwebmp.process.angular.ts

# Runtime environment hints (align with parent hosting configs if used)
ENVIRONMENT=dev
PORT=8080

# CI/CD secrets (leave blank; configure in GitHub Actions/secret store)
USERNAME=
USER_TOKEN=
SONA_USERNAME=
SONA_PASSWORD=
